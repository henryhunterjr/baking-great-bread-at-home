
import React from 'react';

const SupportedFormats: React.FC = () => {
  return (
    <div className="text-center text-sm text-muted-foreground">
      Supported formats: Images (JPEG, PNG), PDF, TXT
    </div>
  );
};

export default SupportedFormats;
